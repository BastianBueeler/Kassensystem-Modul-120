package applicationView;

import java.io.IOException;
import java.net.URL;
import java.util.ResourceBundle;

import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.fxml.Initializable;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.stage.Stage;

public class View2BController {

	@FXML
	private Button Buttergipfeli;
	
	private String View = "B";
	
	private String name;
	private String hname;
	private String artname;
	private String markenname;
	
	@FXML
	protected void nextView_Ap(ActionEvent event) throws IOException {
		
		
		System.out.println("next view!");

		Stage stage;
		Parent root = null;

		FXMLLoader loader = new FXMLLoader(getClass().getResource("/applicationView/View3.fxml"));
		root = (Parent) loader.load();
		
		View3Controller Controller = loader.getController();
		Controller.setName("Apfel");		
		
		stage = (Stage) Buttergipfeli.getScene().getWindow();
		Scene scene = new Scene(root);
		stage.setScene(scene);
		stage.show();
	}
	
	@FXML
	protected void nextView_Bi(ActionEvent event) {
		System.out.println("next view!");

		Stage stage;
		Parent root = null;

	
		stage = (Stage) Buttergipfeli.getScene().getWindow();

		try {
			root = FXMLLoader.load(getClass().getResource("/applicationView/View3.fxml"));
		} catch (IOException e) {
			e.printStackTrace();
		}

		Scene scene = new Scene(root);
		stage.setScene(scene);
		stage.show();
	}
	
	@FXML
	protected void nextView_Ba(ActionEvent event) {
		System.out.println("next view!");

		Stage stage;
		Parent root = null;

	
		stage = (Stage) Buttergipfeli.getScene().getWindow();

		try {
			root = FXMLLoader.load(getClass().getResource("/applicationView/View3.fxml"));
		} catch (IOException e) {
			e.printStackTrace();
		}

		Scene scene = new Scene(root);
		stage.setScene(scene);
		stage.show();
	}
	
	@FXML
	protected void nextView_Tr(ActionEvent event) {
		System.out.println("next view!");

		Stage stage;
		Parent root = null;

	
		stage = (Stage) Buttergipfeli.getScene().getWindow();

		try {
			root = FXMLLoader.load(getClass().getResource("/applicationView/View3.fxml"));
		} catch (IOException e) {
			e.printStackTrace();
		}

		Scene scene = new Scene(root);
		stage.setScene(scene);
		stage.show();
	}
	
	@FXML
	protected void nextView_An(ActionEvent event) {
		System.out.println("next view!");

		Stage stage;
		Parent root = null;

	
		stage = (Stage) Buttergipfeli.getScene().getWindow();

		try {
			root = FXMLLoader.load(getClass().getResource("/applicationView/View3.fxml"));
		} catch (IOException e) {
			e.printStackTrace();
		}

		Scene scene = new Scene(root);
		stage.setScene(scene);
		stage.show();
	}
	
	@FXML
	protected void nextView_Ki(ActionEvent event) {
		System.out.println("next view!");

		Stage stage;
		Parent root = null;

	
		stage = (Stage) Buttergipfeli.getScene().getWindow();

		try {
			root = FXMLLoader.load(getClass().getResource("/applicationView/View3.fxml"));
		} catch (IOException e) {
			e.printStackTrace();
		}

		Scene scene = new Scene(root);
		stage.setScene(scene);
		stage.show();
	}
	
	@FXML
	protected void previousView(ActionEvent event) {
		System.out.println("previous view!");

		Stage stage;
		Parent root = null;

	
		stage = (Stage) Buttergipfeli.getScene().getWindow();

		try {
			root = FXMLLoader.load(getClass().getResource("/applicationView/View1.fxml"));
		} catch (IOException e) {
			e.printStackTrace();
		}

		Scene scene = new Scene(root);
		stage.setScene(scene);
		stage.show();
	}

	}